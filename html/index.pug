doctype html
html
  head
    title Practicing Pug
  body
    h1#mainTitle Hello
    p.firstParagraph.
      Bubucela Jaime troglodita casimuerto
      Tengo miedo tengo frio y tengo miedo
      Los nogareda son guays
    #mainDiv
      input(type="password" name="inpPwd" data-js=`${1 > 2 ? "OK" : "NOT OK!"}`)
    - const myClasses = ["class1", "class2", "class3"]
    div.myDivClass(class=myClasses)
    - const myStyles= {"color": "red", "background-color": "blue"}
    div(style=myStyles)
   

    - const myAttributes = {"src": "myPhoto.png", "alt": "This is my photo"}
    img&attributes(myAttributes)

    input(type="text" disabled=true)
 
